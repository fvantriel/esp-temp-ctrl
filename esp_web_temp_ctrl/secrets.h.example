/**
 * Copy this file to secrets.h and fill in your values.
 * secrets.h is ignored by git and must not be committed.
 */
#ifndef SECRETS_H
#define SECRETS_H

// WiFi
const char* ssid     = "YOUR_WIFI_SSID";
const char* password = "YOUR_WIFI_PASSWORD";

// Shelly Plug S (local HTTP API)
const char* shelly_ip   = "192.168.178.88";  // Your Shelly's IP
const char* shelly_user = "";                 // Optional: leave empty if no auth
const char* shelly_pass = "";

#endif
